/*
  Fallback settings.

  You can override these without editing the code by adding parameters
  to the GitHub Pages URL:

  ?name=Danial
  ?name=Danial&tz=Europe/Berlin
  ?name=Danial&locale=en-US

  When TIME_ZONE is "auto", the browser/device timezone is used.
*/
window.GREETING_CONFIG = {
  NAME: "Danial",
  TIME_ZONE: "auto",
  LOCALE: "en-US",
  QUOTE_API: "https://dummyjson.com/quotes/random"
};
